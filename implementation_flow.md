# 実装フロー — 寿司デッキバトル

> 企画書（`企画書_統合版.md`）との差分と、MVP完成までのロードマップ。
> 2026-07-07 更新：コード実態に合わせて全面改訂（旧版の Phase 1・2 はほぼ実装済みだった）。

---

## 現状スナップショット（2026-07-07）

### ✅ 実装済み

| 領域 | 内容 |
|---|---|
| タイトル・モード選択 | TitleScreen / ModeSelectScreen（CPU戦・二人対戦の分岐あり） |
| ドラフト（CSS版） | ベルト2本 + 特急注文（3回・定価×1.5）+ 購入モーダル + DEVスキップ |
| ドラフト（3D版） | Three.js ベルト、ホバー演出、特急注文 |
| 二人対戦（同端末） | ドラフト→ハンドオフ画面→P2ドラフト→バトル。パス画面でターン交代（p*/c* スワップ方式） |
| バトル基盤 | 手札・机・AP・お腹ゲージ・消化（−5）・ターンフロー・勝敗画面 |
| カード効果 | `kireta_stack` / `kireta_consume_x2` / `self_digest_3` / `digest_stop_1t` / `belly_boost_60/65/70/persist_50` / `chain_on_kaisen_summon` |
| コンボ | 累積型：赤身三種盛り・巻物コンプ / 同時型：海の幸三昧・肉祭り（演出付き） |
| CPU AI（ふつう） | 攻撃力降順の貪欲選択 + digest_stop の使用 |
| 型定義・カードデータ | `types/index.ts` / `cards.ts`（汎用10種 + ビルド系多数） |

### 🔧 2026-07-07 修正済みバグ

- `DraftScreenThree.tsx` — 巻物の切り口生成での誤った型キャスト（tscエラー）
- `PixiConveyorBelt.tsx` — Pixi v7 で削除された `buttonMode` → `eventMode`/`cursor` に置換（tscエラー）
- `BattleScreen.tsx` — 同一カードを2枚以上デッキに入れた場合（特急注文で重複購入可能）、1枚プレイすると手札から全部消えるバグ → indexで1枚だけ除去
- `BattleScreen.tsx` — 手札上限超過時、引いたカードが山札からも手札からも消滅するバグ → 上限を超える分は山札に残す
- `BattleScreen.tsx` — `initState`（シャッフル込み）が毎レンダー実行されていた → 遅延初期化に変更
- `PurchaseModal.tsx` — `belly_boost_65` の効果表記が +8 になっていた（正: +6）
- `BattleScreen.tsx` — 召喚処理を純関数 `applySummon` に共通化。CPUも切れ味スタック・コンボ・攻撃バフ・`self_digest_3` / `kireta_*` を使うようになった
- `BattleScreen.tsx` — CPUの召喚カード（特に即時型）が机に一度も描画されない問題 → 召喚時に表示してから攻撃する2段階フローに変更
- `components/SushiArt.tsx`（新規） — バトル画面のカードを絵文字からSVG描画の寿司（にぎり/軍艦/巻物、ネタ色はbaseごと）に変更。手札・机・詳細シートに適用
- `DraftScreenThree.tsx` — 3D寿司の質感改善：ネタにUV+クリアコート、サーモン/えびの白い筋テクスチャ、たまごの海苔帯、いくら・とびこ・コーン・納豆軍艦はツヤ粒の集合に

---

## ❌ 未実装（企画書との差分）

### 優先度 S — MVPに必須

#### 1. `multi_base` 効果（太巻き）が飾りのみ

太巻きは「複数のbaseコンボ条件を同時に満たせる」はずだが、`playCard` は `card.base`（=太巻き）しか記録しないため、海の幸三昧などの同時型コンボや base 参照処理に一切寄与しない。

**実装方針：** `Card` に `subBases?: string[]` を追加（太巻き: きゅうり・かんぴょう等）。`playCard` の `newThisTurnBases` へ base + subBases を push。海鮮連鎖の判定 `CHAIN_TRIGGER_BASES.has(card.base)` も subBases を見る。

#### 2. 3D版ドラフトの機能不足

- 手動でバトルへ進む手段がない（90秒タイマー待ちのみ）→ CSS版と同じ「バトルへ」ボタンを追加
- 二人対戦時の `playerNum` バッジ非対応（`App.tsx` が渡していない）

#### 3. ~~CPU側のゲームルール不完全~~ ✅ 解消（2026-07-07）

`applySummon` への共通化で対応済み。CPUも効果・コンボ・バフ・切れ味スタックを使用する。

### 優先度 A — MVP品質

#### 4. デッキ枚数の下限チェック

デッキ0枚でもバトルに進める（汎用カードで代替される）。最低枚数（例: 8枚）未満なら警告 or 自動補充を明示する。

#### 5. CPU用ドラフト

CPUデッキは `getCpuDeck()` のハードコード。予算3000円分をアーキタイプ偏重でランダム購入する簡易ドラフトに置き換えると対戦の多様性が出る。

#### 6. 未使用コードの整理

- `DraftScreenPixi.tsx` / `PixiConveyorBelt.tsx`（Pixi版）と `DraftScreen3D.tsx` はどこからも import されていない → 削除 or 実験用フォルダへ
- 整理すれば pixi.js 依存も外せてバンドルが軽くなる

#### 7. `types/index.ts` とバトル state の統一

`GameState` / `Player` / `Combo` 型が定義済みだが `BattleScreen.tsx` は独自の `S` 型を使用。二重管理なので、Phase 1 のロジック切り出しと同時に types 側へ寄せる（または types 側の未使用型を削除）。

### 優先度 B — v1.1

| 項目 | 内容 | 下準備 |
|---|---|---|
| 薬味スロット | あがり/ガリ/サビ。`CondimentSlot` 型は定義済み | UI + 発動ロジック |
| サイドメニュー | ドラフトで取得、バトル開始時にセット | `Player.sideMenu` 型は定義済み |
| おまかせ機能 | ドラフト中に定価−20%でランダム取得 | ボタン1つ + 抽選 |
| 光り物三昧 | 大葉トッピング3枚 → 切れ味+3 | COMBOS に追加（切れ味実装済みなのですぐ可能） |
| 高級三昧 | 500円皿3枚 → 全ステ+1 | COMBOS + 全体バフの仕組み |

### 優先度 C — v2

CPU「むずかしい」（先読み）、GLTF 3Dモデル、オンライン対戦（Supabase）、拡張アーキタイプの数値確定。

---

## 実装順序ロードマップ

```
Phase 1 — ロジック共通化とルール完全化（S）
├── 1-1. ✅ applySummon へ効果・コンボ処理を切り出し（2026-07-07）
├── 1-2. ✅ CPUターンで applySummon を使用（2026-07-07）
├── 1-3. multi_base（subBases）実装
└── 1-4. バランステスト（自動対戦シミュレーション 20 試行）

Phase 2 — ドラフト仕上げ（S〜A）
├── 2-1. 3D版に「バトルへ」ボタン + playerNum 対応
├── 2-2. デッキ下限チェック（8枚未満は警告）
└── 2-3. CPU用簡易ドラフト

Phase 3 — コード衛生（A）
├── 3-1. Pixi版・DraftScreen3D の削除、pixi.js 依存除去
└── 3-2. types/index.ts と S 型の統一（不要型の削除含む）

Phase 4 — v1.1 機能
├── 4-1. 光り物三昧・高級三昧コンボ
├── 4-2. 薬味スロット / サイドメニュー / おまかせ
└── 4-3. 演出強化（攻撃モーション等）

Phase 5 — v2
└── 5-1. CPU むずかしい / GLTF / オンライン対戦
```

---

## 確定済み仕様（旧版の「未確定事項」の決着）

| 項目 | 確定値（コード実装済み） |
|---|---|
| 特急（新幹線）使用回数 | 3回（`SHINKANSEN_TOTAL = 3`） |
| 特急価格 | 定価×1.5、50円単位切り上げ |
| 巻物コンプ条件 | 累積5枚 |
| いかカード | 即時型 `ika_instant`（汎用）と持続・連鎖型 `ika`（ビルド）の2種共存 |
| 高級三昧 | MVPには含めない（v1.1） |

---

## 残タスクチェックリスト

### Phase 1
- [x] 効果・コンボ処理の純関数化（`applySummon`）✅ 2026-07-07
- [x] CPU側の効果・コンボ・スタック対応 ✅ 2026-07-07
- [ ] `multi_base` / `subBases` 実装
- [ ] 自動対戦シミュレーションでバランス確認

### Phase 2
- [ ] 3D版ドラフトに完了ボタン + playerNum
- [ ] デッキ下限チェック
- [ ] CPU用簡易ドラフト

### Phase 3
- [ ] 未使用コンポーネント削除（Pixi版 / DraftScreen3D）
- [ ] 型定義の統一

### Phase 4（v1.1）
- [ ] 光り物三昧 / 高級三昧
- [ ] 薬味スロット / サイドメニュー / おまかせ

---

*このファイルは実装が進むたびに更新してください。*
